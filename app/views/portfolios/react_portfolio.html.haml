%h1 React Portfolio Items:
=link_to "Create new portfolio item", new_portfolio_path

-@react_portfolio_items.each do |portfolio_item|
  %h2
    =link_to portfolio_item.title, portfolio_show_path(portfolio_item)
  %p
    =portfolio_item.subtitle
  %p
    =portfolio_item.body
  -unless portfolio_item.thumb_image.nil?
    %img(src="#{portfolio_item.thumb_image}")
  =link_to "Edit", edit_portfolio_path(portfolio_item)
  =link_to "Delete", portfolio_item, method: :delete, data: { confitm: "Are you sure"}

  %hr
