.col-md-4
  .card{"data-id": "#{portfolio_item.id}"}
    =image_tag portfolio_img(portfolio_item.thumb_image, 'thumb'), width: '100%'
    %h2
      =link_to portfolio_item.title, portfolio_show_path(portfolio_item)
    %p.card-text
      =portfolio_item.subtitle

    =link_to "Edit", edit_portfolio_path(portfolio_item) if logged_in?(:site_admin)
    =link_to "Delete", portfolio_item, method: :delete, data: { confitm: "Are you sure"} if logged_in?(:site_admin)
